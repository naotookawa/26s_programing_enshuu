# 第9章 状態の保存と通信 (p.155-174)

<!--
source_pages: 155-174
source_md_pages: 156-175
chapter_id: 第9章
title: 状態の保存と通信
keywords: 状態の保存と通信, モジュールとパッケージ, データの扱い, 文字列とバイト列, テキストファイルの読み書き, path, CSV, 階層データとdict, 配列や参照を含むdict, JSON: 階層データの文字列表現, HTTP, HTTP での読み込み, データの配布, geojson, numpy, 線形代数, numpy データの保存, 乱数の管理, seed, 状態管理, ファイルからの実行, ソースファイルの作成と実行, 引数の指定, テストの実行, 総合問題, この章の提出課題, Microsoft Windows での環境設定, Gitbash, WSL, MSYS2
-->

## この章の構成

- 9.1 モジュールとパッケージ (p.155)
- 9.2 データの扱い (p.156-162)
- 9.2.1 文字列とバイト列 (p.156)
- 9.2.2 テキストファイルの読み書き (p.157-158)
- 9.2.3 path (p.159)
- 9.2.4 CSV (p.159)
- 9.2.5 階層データとdict (p.160-161)
- 配列や参照を含むdict (p.160-161)
- 9.2.6 JSON: 階層データの文字列表現 (p.162)
- 9.3 HTTP (p.163)
- 9.3.1 HTTP での読み込み (p.163)
- 9.3.2 データの配布 (p.164)
- 9.3.3 geojson (p.164)
- 9.4 numpy (p.164-165)
- 9.4.1 線形代数 (p.165)
- 9.4.2 numpy データの保存 (p.166)
- 9.5 乱数の管理 (p.166-167)
- 9.5.1 seed (p.167)
- 9.5.2 状態管理 (p.167)
- 9.6 ファイルからの実行 (p.168-170)
- 9.6.1 ソースファイルの作成と実行 (p.168)
- 9.6.2 引数の指定 (p.169)
- 9.6.3 テストの実行 (p.170)
- 9.6.4 総合問題 (p.170)
- 9.7 この章の提出課題 (p.171)
- 9.A Microsoft Windows での環境設定 (p.171)
- 9.A.1 Gitbash (p.171)
- 9.A.2 WSL (p.172)
- 9.A.3 MSYS2 (p.172)
- 9.B Jupyter で書いたプログラムの実行 (p.172)
- 9.C ファイルの分割 (p.173)
- 9.D reload (p.174)

## 9.1 モジュールとパッケージ (p.155)

<!-- source: pdf_page=155; md_page_heading=p.156 -->

第 9 章 状態の保存と通信 2026-06-17 Wed 概要 複数のプログラムが協調して動作する場合や，中断した計算を再開するためには，実行の状 態を適当な表現に変換してやりとりする必要がある．テキスト形式のファイルの読み書きと， 少し複雑なデータを json で表現することを紹介する．また，最終課題の準備の一部として， パッケージ管理の導入や，jupyter を離れてコマンドラインから Python を実行する方法を紹介 する． 9.1 モジュールとパッケージ 一般にソフトウェアはさまざまな component から構成される．研究等でプログラムを作成する場合 でも，自分で書く部分を正しく書くだけでなく，既存のソースコードを正しく理解してうまく組み合 わせることも肝要である． Python の module は，ひとまとまりの機能のまとまりで，import で利用可能となる．通常 import x とするとモジュール x を利用可能になる．これまでも，import math として math.sqrt を使うこと を紹介した．また，1 つ以上の module を管理する単位を package と呼ぶ．Package は複数の module を含みうる．1） 必要な機能を探す際は，おおむね以下の順が良い 種別 使い方 例 標準関数 Built-in functions, constants, types な ど bult-in の 項 目 に 掲 載 さ れ て い る も の ．import な し で 使 え る ． https://docs.python.org/3/library/index.html sum, str 標準ライブラリ 上記の URL で，built-in でないもの．import 後に使え る． math, sys, os, urllib． PyPI パッケージ PyPI https://pypi.org/ に登録されているパッケー ジで，pip install するとダウンロード，インストール される．その後に import して使える． matplotlib, numpy その他パッケージ インストール方法がさまざま 潜水艦ゲーム (後述) インストールされているパッケージの一覧は pip list により得られる (jupyter のセルから実行す 1） https://docs.python.org/3/glossary.html

## 9.2 データの扱い (p.156-162)

### 9.2.1 文字列とバイト列 (p.156)

<!-- source: pdf_page=156; md_page_heading=p.157 -->

9.2 データの扱い る場合は !pip list )．また，import 時にパッケージを探すフォルダのリストは，sys.path という変 数に格納されていて，import sys 後にアクセスできる． 

**実行例**

```text
>>> import sys
>>> sys.path
[' /usr/lib/python310.zip' , ..., ' /usr/local/lib/python3.10/site-package' ]
```

(実際の出力は各環境により異なる) パッケージに対応する module について，import 後に __path__ が対応するソースファイルの位置 を示す． 

**実行例**

```text
>>> import numpy as np
>>> np.__path__
[' /usr/local/lib/python3.10/site-package/numpy' ]
```

9.2 データの扱い ファイル保存と読み込みや，ネットワークで送受信する際には，対象となるデータの表現を適切に 選ぶ必要がある． 通信相手 種別 用途 人 テキスト log など 外部 csv 表 json, yaml 階層データ，木構造, dict 内部 npz np.array (ベクトル，行列) pickle Python object ( お勧めしない) 9.2.1 文字列とバイト列 Python や Java, Rust など現代的な言語では，文字列 (string) とバイト列 bytes を区別して扱う． 通常は文字列を扱えば良いが，ネットワーク通信などバイト列が必要になる場合もある．文字列のバ イト列表現を得たり，逆の変換の際には，符号化方式を指定する必要がある． string bytes encode(`utf-8`) decode(`utf-8`) 以下の実行例で \xe3 は 2 桁の 16 進数 e3 を表す．他も同様．

### 9.2.2 テキストファイルの読み書き (p.157-158)

<!-- source: pdf_page=157; md_page_heading=p.158 -->

9.2 データの扱い 

**実行例**

```text
>>> "プログラミング".encode(' utf-8' )
```

b' \xe3\x83\x97\xe3\x83\xad\xe3\x82\xb0\xe3\x83\xa9\xe3\x83\x9f\xe3\x83\xb3\xe3\x82\xb0'2 >>> "プログラミング".encode(' euc-jp' ) 符号化方式は，日本では通常 utf-8 を用いる．過去の日本では，ISO-2022-JP, EUC-JP, Shift JIS な ども使われた．同じ文字でも，符号化方式が異なると，異なった byte 列に符号化される． byte 列は，文字で表現できるものとも限らない．任意の byte 列を ASCII で印字可能な限られた文 字で表現する手法の一つが base64 である．電子メールに画像などを添付する場合や，colab に png を コピーペースト2）した場合など，目にする機会も多い．

#### 問題 Base64 (option) 文字列 s の`utf-8` のバイト列に対する base64 表現を得る， myencode(s) を実装せよ．また， 同関数
で変換した base64 表現から，元の文字列を得る mydecode(s) を実装せよ．自分の姓名 (持っている人は漢字 ) を Base64 でエンコードして表示し，それを復号して元に戻ることを確 認する doctest を付与すること．

**ヒント:**  Python3 では base64 というモジュールが用意されている． import base64 して使用 せよ．モジュールの使い方は help(base64) により調査せよ．ヒント: バイト列を base64 に変 換する関数は base64.standard b64encode(b) である． 

**実行例**

```text
>>> myencode("プログラミング")
' 44OX44Ot44Kw44Op44Of44Oz44Kw'
>>> mydecode(' 44OX44Ot44Kw44Op44Of44Oz44Kw' )
' プログラミング'
```

9.2.2 テキストファイルの読み書き まず，テキストファイルを行単位で読み書きするには，open という関数を用いる． Python3 with open("file-test.txt", "w") as file: 1 file.write("hello\n") 2 file.write("world\n") 3 ここで with で始まるブロックにおいて，file は，open したファイルを指す．ブロックを抜けると， 自動で close される．文中の file.write は，指定したファイルを対象に，引数で指定された文字列の 2） https://en.wikipedia.org/wiki/Data_URI_scheme

<!-- source: pdf_page=158; md_page_heading=p.159 -->

9.2 データの扱い 書き込みを行う． ファイルが作成されたことを「ターミナル」を開いてコマンドで確認しよう．これ以降，ターミナ ルを多用する． macOS のターミナルは， というアイコンでドックに登録されている． Microsoft Windows で同等の環境を用意する方法は 9.A 節 を参照．実行例で， "$" で始まる行は，ユーザ (あな た) が入力するべき行の目印である．ただし， "$" 自身はプロンプトの意図なので入力せず，その右か ら入力し行末でエンターキーをタイプする．使い方は，はいぱーワークブックなどで確認すること．3） 最近の jupyter はターミナル機能を持つので， そちらを使っても良い． cat はファイルの中身を表示するコマンドである．プログラムの動作確認のために， 対応するコマン ドを覚えておくと良い．なお，文字 "#" とそれより右は，コメントの意図なので，入力の必要はない． 

**Terminal**

```bash
$ cat file-test.txt # テスト
```

hello 改行文字や，アスキーコードも含めて確認するためには，od -xc コマンドを使うことができる． 

**Terminal**

```bash
$ od -xc file-test.txt
```

0000000 6568 6c6c 0a6f 6f77 6c72 0a64 ファイルを読み込んで表示する Python のコードは以下のようになる． Python3 with open("file-test.txt", "r") as file: 1 for line in file: 2 line = line.strip() # 改行文字を除去 3 print(line) 4 3） https://hwb.ecc.u-tokyo.ac.jp/hwb2023/information/cui/terminal/

### 9.2.3 path (p.159)

### 9.2.4 CSV (p.159)

<!-- source: pdf_page=159; md_page_heading=p.160 -->

9.2 データの扱い ターミナルの一部の操作は，jupyter のセルから ! 記号を用いて行うこともできる． 

**実行例**

```text
>>> !cat file -test.txt
hello
world
```

9.2.3 path macOS も含めて多くの OS で /tildelowはホームディレクトリの表記に用いられる．一方で， (慣習から外 れて) /tildelowそのものを名前とするファイルやフォルダも扱うことができるので注意が必要である．ホーム ディレクトリとしての表記を，実際のパスに変換するためには， os.path.expanduser 関数を用い る．たとえば，ダウンロードフォルダ内の filename.txt を open する際は，次のように変換してか ら用いる． 

**Python3**

```python
import os.path
```

os.path.expanduser(' ˜/Downloads/filename.txt' ) 行を表示するコード片である．なお，文字コードが Shift JIS なので open の際に encoding を指定し ている． 

**実行例**

```text
>>> import csv
>>> import os.path
>>> with open(os.path.expanduser(' ˜/Downloads/viewpointmap.csv' ), encoding=' sjis' ) as file:
... reader = csv.reader(file)
... rows = [_ for _ in reader]
... for i in range(3):
... print(rows[i])
```

4） https://data.city.kobe.lg.jp/

### 9.2.5 階層データとdict (p.160-161)

#### 配列や参照を含むdict (p.160-161)

<!-- source: pdf_page=160; md_page_heading=p.161 -->

9.2 データの扱い [' 名前' , ' 経度' , ' 緯度' , ' 説明' ] 8 [' 01_保久良神社（１０選）' , ' 135.278118' , ' 34.734791' , ' 金鳥山の中腹にあ る... 9 CSV モジュールの詳細は，標準文書 https://docs.python.org/3/library/csv.html を参照さ れたい．なお，大きな表データを扱う際には，pandas や polar などの外部ライブラリを使うと，メモ リ効率などの点でメリットがある． 9.2.5 階層データと dict Python の dict (辞書，マッピング， 連想配列) は，データの組 (key, value) の組を保持し， key か ら value を容易に検索することができる．別の言い方をすると，配列の添字は整数だが，連想配列で は整数以外も添字のように使える． (以前二分木のところで簡単に紹介した )．他の言語でも C++の unorderd map や Ruby のハッシュなどで同等の機能が提供されている． 連想配列は，{} で構築する (配列を [] で構築したことと似ている )．キーに対応するデータの登録 や参照は，配列と同様の文法を用いる．キーが登録済みかどうかは， キー in 連想配列という構文で 調べることができる． 

**実行例**

```text
>>> dic = {} # 連想配列の構築
>>> dic[' hello' ] = ' kon-nichi-ha' # データの登録
>>> dic
{' hello' : ' kon-nichi-ha' }
>>> ' hello' in dic # key "hello" は今登録したばかり
True
>>> ' good morning ' in dic
False
>>> dic[' hello' ] # データの参照
' kon-nichi-ha'
>>> dic[' four' ]
Traceback (most recent call last):
File "<stdin>", line 1, in <module>
KeyError: ' four'
>>> dic[' four' ] =
# 文字列以外の型も使用可能
>>> dic[' four' ]
```

配列や参照を含む dict dict には，配列や別の dict を登録することができる． 例として，階層ディレクトリのフォルダを dict で表すことを考える．あるフォルダには， (フォル ダでない) ファイルと，フォルダがあるので，それぞれ"files"と"folders"で表すとする 空のディレクトリを表すオブジェクトは以下のように構築できる．

<!-- source: pdf_page=161; md_page_heading=p.162 -->

9.2 データの扱い 

**実行例**

```text
>>> a = {}
>>> a["files"] = []
>>> a["folders"] = []
>>> a
{' files' : [], ' folders' : []}
```

以下のように，dict の構築時に key, value を: 記号でつないで，データを指定することもできる． 

**実行例**

```text
>>> b = {"files":[], "folders":[]}
>>> b
{' files' : [], ' folders' : []}
```

以下の図のような，階層関係を構築してみよう． ὜0a a.txt ὜0b hello.txt 

**実行例**

```text
>>> b[' files' ].append(' hello.txt' )
>>> b
{' files' : [ ' hello.txt' ], ' folders' : []}
>>> a[' files' ].append(' a.txt' )
>>> a
{' files' : [ ' a.txt' ], ' folders' : []}
>>> a[' folders' ].append(b)
>>> a
{' files' : [ ' a.txt' ], ' folders' : [{ ' files' : [ ' hello.txt' ], ' folders' : []}]}
>>> b
{' files' : [ ' hello.txt' ], ' folders' : []}
```

テスト例: 

**実行例**

```text
>>> a[' files' ][0]
' a.txt'
>>> a[' folders' ][0]
{' files' : [ ' hello.txt' ], ' folders' : []}
>>> a[' folders' ][0][' files' ][0]
' hello.txt'
```

### 9.2.6 JSON: 階層データの文字列表現 (p.162)

<!-- source: pdf_page=162; md_page_heading=p.163 -->

9.2 データの扱い 9.2.6 JSON: 階層データの文字列表現 さて，階層構造を含むようなデータは，行でデータを区切るテキストファイルで表現することが難 しい．そこでウェブを中心に広く使われている JSON という表現形式を紹介する． Python ではまず json ライブラリを読み込む． 

**Python3**

```python
import json
```

これにより，Python のオブジェクトを JSON 形式の文字列に変換する json.dumps 関数が使えるよ うになる． 

**実行例**

```text
>>> json.dumps([3,1,4])
' [3, 1, 4] '
```

先ほどのディレクトリ a のような構造を持つデータも，文字列に変換される． 

**実行例**

```text
>>> json.dumps(a)
' {"files": ["a.txt"], "folders": [{"files": ["hello.txt"], "folders": []}]}'
```

変換された文字列は，json.loads という手続きにより元のデータを取り出すことができる． 

**実行例**

```text
>>> json.loads(' [3, 1, 4] ' )
[3, 1, 4]
```

#### 問題 二分木の保存 (write tree, read tree) 6.1.6 項 "内部表現の世界と使用する世界の分離" では二分木を dict で表現した．二分木
 (たと えば Q&A で作成したもの) をファイルに保存する関数 write tree(filename, tree) と読み 込んで返す関数 read tree(filename) を作成せよ．同じ木が復元できていることを確認せよ． ただし，二分木の表現としては以下を使うと良い．これらは 6.1.6 項のものと同じである． 

**Python3**

```python
def make_node(num, left, right):
    return {"number": num, "left": left, "right": right}
def value(tree):
    return tree["number"]
def left(tree):
```

## 9.3 HTTP (p.163)

### 9.3.1 HTTP での読み込み (p.163)

<!-- source: pdf_page=163; md_page_heading=p.164 -->

9.3 HTTP return tree["left"] 8 9 def right(tree): 10 return tree["right"] 11 9.3 HTTP 9.3.1 HTTP での読み込み Python では urllib.request を読み込んでおくと，open と同様にして HTTP 読み込みが可能にな る．5） 

**Python3**

```python
import urllib.request
```

with urllib.request.urlopen("https://www.ecc.u-tokyo.ac.jp/") as response: Terminal <!DOCTYPE html> 1 <html lang="ja"> 2 <head> 3 <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"> 4 <link rel="shortcut icon" href="/favicon.ico"> 5 <meta name="date" content="2018-04-26T12:03+09:00"> 6 <meta name="viewport" content="width=device-width,initial-scale=1.0"> 7 ... 8 </head> 9 </html> 10

#### 例題 HTTP 読み込み 指定した URL の内容を，指定したファイルに保存する関数 httpget(url, filename) を作成 せよ． なお，同様の操作は
 curl というコマンドを用いて以下のように行うことができる．また wget とい う似た機能を持つコマンドも広く使われている． 5） https://docs.python.jp/3/howto/urllib2.html

### 9.3.2 データの配布 (p.164)

### 9.3.3 geojson (p.164)

## 9.4 numpy (p.164-165)

<!-- source: pdf_page=164; md_page_heading=p.165 -->

9.4 numpy 

**Terminal**

```bash
$ curl -o filename ' http://...'
```

9.3.2 データの配布 データの配布を http と json で実現することも多い． たとえば，郵便番号と住所の対応のデータは元々公開されているが，市町村の合併などで変更があ るため，郵便番号を用いるソフトウェアは常に更新に気を配る必要がある．検索を容易にするために， json でデータを配布する有志のサイトもある．6）

#### 問題 住所の表示 (showaddress; option) 便番号をあらわす文字列， ' 1538902' などから該当する URL の json を入手し，住所を
表示す るプログラムを書いてみよう． なお，他サイトのサーバの負担にならないように，プログラムの動作検証を終えるまでは，担 当教員が複製した以下の URL でまずは練習すること．
- https://www.graco.c.u-tokyo.ac.jp/˜kaneko/lecture/pl/1538902.json
- https://www.graco.c.u-tokyo.ac.jp/˜kaneko/lecture/pl/1138654.json 9.3.3 geojson GeoJSON は，比較的新しく制定されたフォーマットで，位置を含むデータを扱う．ライブラ リ を 組 み 合 わ せ る とGIS の 前 提 知 識 を ほ ぼ 必 要 と せ ず に ，地 図 に マ ー カ を 描 く こ と が で き る ． https://gis-oer.github.io/gitbook/book/materials/web_gis/GeoJSON/GeoJSON.html 詳しくは別資料を参照 https://colab.research.google.com/drive/1osriLjeM1zGSHKIV4YsYXZm-xpfgC6e9 9.4 numpy Python で数値計算を行う場合は，numpy というライブラリが用意されている．7） 以下は，numpy で 3x4 の行列を作成する例である． 

**Python3**

```python
import numpy as np
a = np.zeros((3,4))
```

**実行例**

```text
>>> a
array([[0., 0., 0., 0.],
```

6） 例 https://github.com/ttskch/jp-postal-code-api 7） https://numpy.org/doc/stable/user/quickstart.html

### 9.4.1 線形代数 (p.165)

<!-- source: pdf_page=165; md_page_heading=p.166 -->

9.4 numpy [0., 0., 0., 0.], 3 [0., 0., 0., 0.]]) 4 1 行目の import numpy as np は，np という別名で，numpy モジュールにアクセスできるようにす る構文である (numpy を np という別名で扱う慣習に，本資料も倣った)． 巨大なベクトル 可読性を維持するため，大きなベクトルや行列は，適宜省略して表示される．な お，下のコード例の途中の+=1 は，全要素に対して 1 を加える演算である． 

**実行例**

```text
>>> large_array = np.zeros(10000)
>>> print(large_array)
[0. 0. 0. ... 0. 0. 0.]
>>> large_array +=
>>> large_array
array([1., 1., 1., ..., 1., 1., 1.])
```

9.4.1 線形代数 行列の乗算には @ 記号が用意されている8）．       c の計算例を示す． 

**実行例**

```text
>>> A = np.array([[8,3,4], [ 1,5,9], [ 6,7,2]])
>>> b = np.array([1,2,3])
>>> c = A@b
>>> c
array([26, 38, 26])
```

線形代数ライブラリは np.linalg にあり，Ax = c を解いて x を得るには np.linalg.solv を用 いる． 

**実行例**

```text
>>> np.linalg.solve(A, c)
array([1., 2., 3.])
```

逆行列は inv で求める．ただし，逆行列の用途が連立式 Ax = c をとくことの場合は，数値計算の 誤差の観点から，前述の np.linalg.solv(A, c) を使う方が良い． 8） Python 3.5 以降 https://docs.python.org/ja/3/whatsnew/3.5.html#whatsnew-pep-465

### 9.4.2 numpy データの保存 (p.166)

## 9.5 乱数の管理 (p.166-167)

<!-- source: pdf_page=166; md_page_heading=p.167 -->

9.5 乱数の管理 

**実行例**

```text
>>> A=np.array([[1,2], [ -3,4]])
>>> np.linalg.inv(A)
array([[ 0.4, -0.2],
[ 0.3, 0.1]])
```

固有値，固有ベクトルを求める: 

**実行例**

```text
>>> np.linalg.eig(A)
(array([2.5+1.93649167j, 2.5-1.93649167j]),
array([[0.38729833-0.5j, 0.38729833+0.5j],
[0.77459667+0.j , 0.77459667-0.j ]]))
```

9.4.2 numpy データの保存 複数の numpy オブジェクト (行列やベクトル) を一つのファイルに圧縮して保存することが出来る． ここでは npz という numpy の圧縮形式で保存する，np.savez compressed を紹介する．9）個別にテキ ストファイルや json で保存するよりも効率が良いと期待できる．保存の際の最初の引数はファイル名 で続けて，名前=変数の形式で名前を指定する．読み込みは，np.load で行い，帰ってきた object に同 じ名前でアクセスすると，元のデータを得られる． Python3 np.savez_compressed("filename.npz", matrix =a, vector =large_array) 1 

**実行例**

```text
>>> loaded_objects = np.load("filename.npz")
>>> loaded_objects["vector"]
array([1., 1., 1., ..., 1., 1., 1.]) # 保存時の large array に相当
```

9.5 乱数の管理 疑似乱数に関係する機能は random module で提供されている．指定の確率分布に従う乱数の生成 や，シャッフルなど，さまざまな機能が提供されているので，必要に応じて標準文書を参照されたい． https://docs.python.org/3/library/random.html 

**実行例**

```text
>>> import random
>>> random.random() # [0,1) 一様分布
0.43401144301524397
>>> random.randrange(200) # [0, 199] の整数
```

9） https://numpy.org/doc/stable/reference/generated/numpy.savez_compressed.html

### 9.5.1 seed (p.167)

### 9.5.2 状態管理 (p.167)

<!-- source: pdf_page=167; md_page_heading=p.168 -->

9.5 乱数の管理 >>> a = [' orange' , ' apple' , ' banana' , ' pear' ] 6 >>> random.choice(a) 7 ' banana' 8 >>> random.sample(a, 2) 9 [' apple' , ' orange' ] 10 なお，numpy を使う場合は，np.random を使う． 

**実行例**

```text
>> import numpy as np
>> np.random.randint(1, 6, size=10) # サイコロを
回ふった
array([5, 5, 2, 5, 5, 4, 2, 1])
```

9.5.1 seed 計算機シミュレーションにおける再現性を確保したい場合など，疑似乱数の系列を制御したい場合 もある．random.seed を使うと状態を制御できる． 初めに seed(0) を指定して，random.random() で乱数列を生成したとして， 

**実行例**

```text
>>> random.seed(0)
>>> print(random.random())
0.8444218515250481
>>> print(random.random())
0.7579544029403025
```

リセットしたいタイミングで seed(0) を呼ぶと，random.random() で生成される列が，もとと同 じになる． 

**実行例**

```text
>>> random.seed(0)
>>> print(random.random())
0.8444218515250481
>>> print(random.random())
0.7579544029403025
```

numpy.random.seed も同様． 9.5.2 状態管理 乱数を使うモジュールが複数ある場合， (たとえば，別に開発されたプレイヤーを対戦させる場合な ど)，乱数列はそれぞれ管理することが望ましい．たとえば，random.seed を複数のモジュールから呼 んでしまうと，意図する動作にはほぼならない． 推奨する方法は，random.Random(seed ) により疑似乱数発生オブジェクトを作り，そのオブジェク トから乱数を発生させることである．以下の例では，rng a がオブジェクトで，random や randrange

## 9.6 ファイルからの実行 (p.168-170)

### 9.6.1 ソースファイルの作成と実行 (p.168)

<!-- source: pdf_page=168; md_page_heading=p.169 -->

9.6 ファイルからの実行 など，これまでに紹介した関数と同名のメソッド・機能を利用できる．この乱数の発生状態は，他の 乱数生成の影響を受けない． 

**実行例**

```text
>>> rng_a = random.Random(0)
>>> rng_a.randrange(200)
>>> rng_a.random()
0.7579544029403025
```

np.random.RandomState も同様の機能を持つ． 9.6 ファイルからの実行 これまでの演習では，標準環境として jupyter を指定し，ブラウザ上で動作するインタープリター上 で実習してきた．この方法は，比較的学習に適しているが，サーバや長時間の計算など無人で動かす 用途には向かない． 標準的には， 「ターミナル」から，python3 コマンドを実行する．最終課題 (第 B 章) に備えて，動 作環境を確認し設定しよう． まずは，バージョンを確認しよう．Python 3.x と表示されればこの資料の意図の通りである． 

**Terminal**

```bash
$ python3 --version
```

Python 3.10.9 (余談) 次のコマンドは，簡易のウェブサーバをたてて，カレントフォルダ以下をウェブアクセス可 能にする． 

**Terminal**

```bash
$ python3 -m http.server
```

9.6.1 ソースファイルの作成と実行 VSCode などのエディタで，次の内容の Python ソースファイルを hello.py という名前で作る． 

**Python3**

```python
print(' Hello' )
```

そのフォルダにターミナルで移動する．もしくは， VSCode のターミナルを開く．10）cat コマンド で，内容がエディタで保存した内容と同一であることを確認する． 10） https://code.visualstudio.com/docs/terminal/getting-started

### 9.6.2 引数の指定 (p.169)

<!-- source: pdf_page=169; md_page_heading=p.170 -->

9.6 ファイルからの実行 

**Terminal**

```bash
$ cat hello.py
```

print(' Hello' ) ソースコードのファイル名を指定して実行する． 

**Terminal**

```bash
$ python3 hello.py
```

Hello Help:ファイル名の注意 フ ァ イ ル 名 は 標 準 的 な ラ イ ブ ラ リ と 重 な ら な い よ う に つ け る と 良 い ．た と え ば ，numpy と 関 連 が 深 い 自 分 の プ ロ グ ラ ム が あ っ た と し て ，フ ァ イ ル 名 前 ，numpy.py で は な く myprojectname numpy.py などとする． 9.6.2 引数の指定 実行時にソースファイル名に続いて，引数を渡して，プログラムの動作を制御することができる．

#### 例題 整数の和 引数で与えられた，二つの整数の和を求めるプログラムを作って見よう 引数は，sys.argv という配列から入手できる．型は文字列 str なので，整数
として使う場合は int で変換する．以下は実装例である． 

**Python3**

```python
import sys
def add(x, y):
```

''' return sum of x and y

### 9.6.3 テストの実行 (p.170)

### 9.6.4 総合問題 (p.170)

<!-- source: pdf_page=170; md_page_heading=p.171 -->

9.6 ファイルからの実行 if __name__ == ' __main__' : 18 a = int(sys.argv[1]) 19 b = int(sys.argv[2]) 20 print(add(a, b)) 21 18 行目の if __name__ == ' __main__' : のブロックがメインの計算で，この if の行をこのまま， 以後，つけておくことを推奨する．省略可能の場合もあるが，省略すると次に紹介するテストの実行 や multiprocessing で並列処理を行う場合などで問題が起こりうる． 9.6.3 テストの実行 ファイルに保存した Python のソースコードについて， doctest ( 8.2 節) を実行する場合は，ターミ ナルから次のようにタイプする． 

**Terminal**

```bash
$ python3 -m doctest -v filename.py
```

pytest 用に書かれたテスト関数を呼ぶには，以下のように実行する． 

**Terminal**

```bash
$ python3 -m pytest filename.py
```

9.6.4 総合問題

#### 問題 showaddress.py 題「住所の表示」で扱った形式の json ファイルが，カレントフォルダにあるとする．引数で指 定されたファイルをひらき，(1) 住
所を 行で表示し，(2) 次の 1 行に同住所の文字数を表示す るプログラムを作成せよ． json から 1 行に連結した文字列を得る機能は，関数として作成し， doctest または pytest に対応するテストを書くと良い． (option) ファイル名の代わりに URL が指定された場合は，ダウンロードしてファイルに保存 する． 実行例 

**Terminal**

```bash
$ python3 showaddress.py 1538902.json
```

東京都目黒区駒場３丁目８−１東京大学大学院総合文化研究科・教養学部

## 9.7 この章の提出課題 (p.171)

## 9.A Microsoft Windows での環境設定 (p.171)

### 9.A.1 Gitbash (p.171)

<!-- source: pdf_page=171; md_page_heading=p.172 -->

9.7 この章の提出課題 9.7 この章の提出課題 Python の 1 つ以上の問題について取り組み， UTOL に提出する．テストを書きにくい問題につい ては，実行結果 (の一部) をコピーペーストで添えることで，テストの代わりとする． 9.A Microsoft Windows での環境設定 Windows 環境で学習している場合は， 自身でターミナル環境とPython 環境を用意する必要がある． 本資料では以下のどちらかの方法を勧める: 1. gitbash + python, 2. Windows Subsystem for Linux WSL 3. MSYS2 9.A.1 Gitbash
- Gitbash を https://gitforwindows.org/ からダウンロード・インスートールする．
- Python を https://www.python.org/downloads/windows/ からダウンロード・インスートール する．2025 年時点で 3.13 シリーズが最新だが，安定版としてその前のバージョンの 3.12.10 また は 3.11.9 を勧める．インストーラにも種類があるが，通常の Intel または AMD の CPU なら， Windows installer (64-bit) が適するはず．
- ターミナルを開く エクスプローラーの右クリックから，ソー スコードを置いたフォルダを gitbash で開く メニューを使うと，ターミナルウィンドウが 開き目的のフォルダに cd した状態となる． あ る い は ， gitbash ア プ リ ケ ー シ ョ ン を 開いてから，ターミナルウィンドウ内で cd /c/Users/your-name/path/to/source/ など でも良い (your-name 以下は適宜書き換えるこ と)．

### 9.A.2 WSL (p.172)

### 9.A.3 MSYS2 (p.172)

## 9.B Jupyter で書いたプログラムの実行 (p.172)

<!-- source: pdf_page=172; md_page_heading=p.173 -->

9.B Jupyter で書いたプログラムの実行
- python コマンドがあるので， python your-file.py などと実行する 9.A.2 WSL
- Powershell を 「管理者権限で実行」
- wsl --install で Ubuntu 22.04 あるいは 24.04 をインストール
- スタートメニューに増えた，Ubuntu というアプリケーションを実行．ターミナルが開く
- ここでは python3 というコマンドが使える．python3 your-file.py などと実行する
- エクスプローラーで見えるホームに移動するには，cd /mnt/c/Users/your-name/ など. 9.A.3 MSYS2 https://www.msys2.org/ インストール 手順したがって，ターミナルが開くまで勧める (default で 進めて良い)． 9.B Jupyter で書いたプログラムの実行 Jupyter で開発したノートブックから Python のソースコード (.py) を出力するには以下のように 行う． 1. jupyter 上で新規ノートブックを作成する 2. ノートブックを編集する
- jupyter ロゴの右をクリックして，ノートブックの名称を "hello" にする
- Hello, World を表示する文を書く 3. python ファイルに export する: File Ô Download as Ô Python(.py)

## 9.C ファイルの分割 (p.173)

<!-- source: pdf_page=173; md_page_heading=p.174 -->

9.C ファイルの分割 ダウンロードフォルダなど (ウェブブラウザの設定と操作による ) に保存されているので， cat コマ ンドで確認し，つづいて，python3 コマンドで実行する． 

**Terminal**

```bash
$ cat hello.py
```

# coding: utf-8 

**Terminal**

```bash
$ python3 hello.py
```

Hello, World 9.C ファイルの分割 大きなプログラムの開発では，複数のファイルに分けて開発して，統合して使用することが多い． 一つのファイルに沢山の行数を詰め込むと見通しが悪くなるため，機能毎に適宜分割することは良い 実践である． 以下の関数が，mymessage.py というファイルに保存されていたとする． 

**Python3**

```python
def nice_message():
    return "Hello, World!!!!! "
```

上記の関数を同一ディレクトリ内の main.py から呼ぶ方法ためには以下のように書く． 

**Python3**

```python
import mymessage
print(mymessage.nice_message())
```

## 9.D reload (p.174)

<!-- source: pdf_page=174; md_page_heading=p.175 -->

9.D reload 

**Terminal**

```bash
$ python3 main.py
```

Hello, World!!!!! この状態で，mymessage.py を (元気よく) 書き換えたとすると， 

**Terminal**

```bash
$ cat mymessage.py
```

def nice_message(): main.py を書き換えていないにもかかわらず，実行結果が変化する．確認せよ． 

**Terminal**

```bash
$ python3 main.py
```

Hello, Hello, Hello, Hello, Hello, World!!!!! 9.D reload jupyter 上でも import mymessage などとして，Python ソースファイルを読み込んで使うこともで きる．一つ注意として jupyter 上で自作ファイルを import 後に元ファイルを変更した場合には，その ままでは新しいファイルの内容は使われない．ファイルを読み直して新しい内容を反映させるために は，importlib を import したうえで，その reload という機能を用いる． 

**Python3**

```python
import importlib
```

importlib.reload(modulename) 別の方法として， jupyter ( の元の ipython) の拡張機能 11）を使い，次の行を実行しておくことでも 良い． Python3 %load_ext autoreload 1 %autoreload 2 2 11） http://ipython.org/ipython-doc/stable/config/extensions/autoreload.html#module-IPython.extensions. autoreload
