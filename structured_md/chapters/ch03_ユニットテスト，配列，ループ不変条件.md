# 第3章 ユニットテスト，配列，ループ不変条件 (p.43-63)

<!--
source_pages: 43-63
source_md_pages: 44-64
chapter_id: 第3章
title: ユニットテスト，配列，ループ不変条件
keywords: ユニットテスト，配列，ループ不変条件, テストの自動化: ユニットテスト, テストケースの作成, 配列と関数, 関数内の変数(前半は再掲), 配列, 変数の値の書き換えとループ不変条件, 配列とループ, 配列や文字列の変更, 発展: 関数のパラメータ化, この章の提出課題, テスト駆動開発?, 閏年の判定, 失敗するテストの作成, 実装とテストの拡充, 実装の変更, 仕様の変更, C++でのテスト
-->

## この章の構成

- 3.1 テストの自動化: ユニットテスト (p.43-46)
- 3.1.1 テストケースの作成 (p.46)
- 3.2 配列と関数 (p.47-49)
- 3.2.1 関数内の変数(前半は再掲) (p.47)
- 3.2.2 配列 (p.48-49)
- 3.3 変数の値の書き換えとループ不変条件 (p.50-51)
- 3.4 配列とループ (p.52)
- 3.5 配列や文字列の変更 (p.53-56)
- 3.6 発展: 関数のパラメータ化 (p.57-58)
- 3.7 この章の提出課題 (p.59)
- 3.A テスト駆動開発? (p.60-61)
- 3.A.1 閏年の判定 (p.60-61)
- 失敗するテストの作成 (p.60)
- 実装とテストの拡充 (p.61)
- 実装の変更 (p.61)
- 仕様の変更 (p.61)
- 3.B C++でのテスト (p.62-63)

## 3.1 テストの自動化: ユニットテスト (p.43-46)

<!-- source: pdf_page=43; md_page_heading=p.44 -->

第 3 章 ユニットテスト，配列，ループ不変条件 Unit test, Array and Loop invariant 2026-04-22 Wed 概要 前週は「関数的に」 (functional) プログラムを書くことをおすすめした．今週は， 「命令的」 (imperative) な文法を再び導入する．これによりプログラムを書く自由度が向上するが，一方 で，プログラムが複雑になり理解が困難となるリスクもある．そこで，プログラムを分かりや すく保つための方法を合わせて身に付けよう．最初に紹介する「 ユニットテスト 」(unit test) は，プログラムを意図通り作成できたかを自動で検査する方法を提供する．続いて，配列と変 数 (値の変更を許す) を題材にループ不変量 (loop invariants) という概念を学ぶ． 3.1 テストの自動化: ユニットテスト 先週は，関数を作成した後，jupyter 上でテストすることを行った． (再掲)

#### 例題 長方形の面積 二辺の長さがそれぞれ a,b である長方形の面積をもとめる関数 rectarea(a,b) を定義せよ 関数をデバッグしたり，あるいは完成したあと
にも高速化など変更を加える際など，同じテストを 繰り返し確実に行いたい．そこで，テストの作業を自動化しよう． この資料では Python の pytest というフレームワークを紹介する．1） まずは，assert の基本機能を紹介し，その後， pytest と jupyter 用の ipytest を紹介する．別途 指示をするまで今週以降は，ipytest を用いること． 

**Python3**

```python
def rectarea(a,b):
    return a*b
# 名前を"test_"で始める
def test_area():
```

1） 2020 年までは unittest を使っていたが変更した

<!-- source: pdf_page=44; md_page_heading=p.45 -->

3.1 テストの自動化: ユニットテスト assert rectarea(7,5) == 35 6 assert rectarea(10,20) == 200 7 関数 test area 内の 2 つの assert 文が本質である．それぞれの行を，具体的な値で計算結果を検 証する，テストケースとよぶ． テストを確認するもっとも単純な方法は，この関数を実行すれば良い． 

**Python3**

```python
test_area()
```

実行が正常に終了した場合は何も表示されない．失敗した場合は，以下のような AssersionError が表示される． 

**実行例**

```text
>>> test_area()
Traceback (most recent call last):
AssertionError:
...
def test_area():
...
assert rectarea(7,5) ==
```

以上が，assert の基本的な機能である．テストケースに限らず，デバッグ等でも有用である． pytest は，これらのテストケースを自動で発見して実行するなど，テスト実行や管理をより便利に するツールである．ipytest は，pytest を jupyter に対応させたもので，以下のように用いる まず，pip module をインストールする．この作業は一度だけ行えば良い． 

**Terminal**

```bash
$ pip3 install ipytest
```

インストールの確認は，たとえば以下のように行う 

**Terminal**

```bash
$ pip3 list
```

ipytest 0.9.1 Microsoft Windows で anaconda を使っている場合， スタートメニューから"Anaconda Prompt" で シェルを起動し，conda コマンドを利用してインストールすることができる．2） Terminal conda install -c conda-forge ipytest 1 続いて，jupyter を再起動し，冒頭で以下を実行する．この作業は各 notebook 毎に行う． 2） https://anaconda.org/conda-forge/ipytest

<!-- source: pdf_page=45; md_page_heading=p.46 -->

3.1 テストの自動化: ユニットテスト 

**Python3**

```python
import ipytest
```

ipytest.autoconfig() これ以降，各 jupyter の cell では，テスト関数の自動実行が可能である．そのためには，セルの先 頭で%%ipytest -qq を記述し (この文法の紹介は割愛する )，テスト関数を test で始まる名前で定義 する Python3 %%ipytest -qq 1 def test_area(): 2 assert rectarea(7,5) == 135 3 assert rectarea(10,20) == 200 4 正常に実行された関数の数だけ . が表示される． 

**実行例**

```text
. [100%]
```

もしテストに失敗すると，以下のように出力される． 

**実行例**

```text
F [100%]
========================== FAILURES ===========================
__________________________ test_area __________________________
def test_area():
> assert rectarea(7,5) ==
E assert
==
E + where
= rectarea(7, 5)
(以下略)
```

テスト駆動開発 という開発スタイルでは，失敗するテストを積極的に利用して開発を勧める (付

### 3.1.1 テストケースの作成 (p.46)

<!-- source: pdf_page=46; md_page_heading=p.47 -->

3.1 テストの自動化: ユニットテスト 録 3.A を参照)． 3.1.1 テストケースの作成 何をテストすれば良いかは関数の複雑さに依存する．関数 rectarea を乗算だけで作成していれば， 1, 2 のケースでほぼ十分であろう．(ただしテストケースを欺く， 「仕様を満たしていないのに，テスト ケースでは検出できない実装」が存在することは認識しておく必要がある．) 

**Python3**

```python
def area_fake(a,b): # こんなコードは来ないことを前提としている
if a == 5:
```

return さっそくテストケースを自分で作ってみよう．

#### 例題 issueCC–テストケース実行 以下の (先週扱った ) 問題について issueCC(3000000,0,10000000) は，(借金返済額 /年収が 3
0%以上であるから) False を返すことが期待される． assert issueCC(3000000,0,10000000) == False 1 が実行されるテストを書いて，テストをパスすることを確認せよ．もし失敗した場合は，関数 issueCC の定義を修正し，再度テストを実行し，テストをパスすることを確認せよ．

#### 問題 信用 (再掲) (年収に対する ) 借金返済額 dt，返済遅延回数 dl，年収の額 ic に対して，以下の決 定木に従って，クレジットカードを発行するかしないか
を true か false で返す関数 issueCC(dt,dl,ic) を定義せよ． ̓ɻ͈̍`ʣ ̔̔ ୈ̐ᡗσʔλͷѻ͍ ̐ɾ ʗ೥ऩ ̐`̞ ©` ʜʜʗ΁ɾɾ ឺ ਤ̐ɽ̎̑ήʔϜ໦ ͠ͳ͍ Ե ໼ ஗Ԇճ਺ ឺ ͠ͳ͍ ʗ ๧ೋɻ ɾɾ̍͜ʀ͜͜͜ཚ ɻɻɻ ͠ͳ͍ ఆ໦ ͔ΒͨͲΔ͜ ஗Ԇճ਺̍ճͷਓʹ͸ɼΫϨδο ͞Εͳ͍͜ͱ͕Θ͔Δɽ Ϟσϧ ࣄ ܎ؔ ɼλϓϧɼϨίʔυͳͲͱ ʯ ͸දͷ࿦ཧతͳ ΘΕΔɽ ʛ

## 3.2 配列と関数 (p.47-49)

### 3.2.1 関数内の変数(前半は再掲) (p.47)

<!-- source: pdf_page=47; md_page_heading=p.48 -->

3.2 配列と関数

#### 例題 Range–テストケース作成 (test range) 以下の例題と回答例に対して， (1) もし正しく実装していれば成功するが，回答例のプログラ ムは失敗する
，というテストケースを作成せよ． (2) 実際にテストを実行し，失敗することを 確認せよ．(3) 関数 range の実装例を修正し，テストが成功することを確認せよ．

#### 例題 Range ( 再掲) ３つの整数 a, b, c に対して，それらが a < b < c の条件を満たすならば True を，満た さないならば False 
を返す関数 in range(a,b,c) を作成せよ

**回答例:**  def in_range(a,b,c): 1 return a < b or b < c # 間違い 2 関数 range については，a,b,c の大小関係や等号の有無に関して，複数のケースを確認する方がよ り安心である．

#### 問題 Circle in a Rectangle– テストケース作成 (test in rectangle) 先週作成した Circle in a Rectangle
 に対する適切なテストケースを作成せよ．

**ヒント:**  この場合は，(i) 円が長方形のなかに完全に収まっていて交わりがない (ii) 外側のとても遠 くにあって交わりがない，(iii)–(vi) 長方形の上下左右の各辺で交わる，の 6 通りをテストすること (一 部は白文字で書いてある)． 今週以降の課題提出では，ほとんどの問題に対して，テストケースを添えることが条件となる．関 数と同時にテストケースを作っておくこと． 3.2 配列と関数 3.2.1 関数内の変数 (前半は再掲) 関数の中でも，値に名前をつける目的で変数を使うことができる． 

**Python3**

```python
def 名前 (仮引数 1, 仮引数 2...):
```

変数

### 3.2.2 配列 (p.48-49)

<!-- source: pdf_page=48; md_page_heading=p.49 -->

3.2 配列と関数 このように，変数は式に別名を与えるもので，その値は関数内で一意に定まるという使い方を基本 とする． Help:NameError 定義していない名前の変数を用いると (e.g., 名前を間違えると)，NameError が発生する >>> Pi = 3.14 1 >>> Pi 2 3.14 3 >>> Pi2 4 Traceback (most recent call last): 5 NameError: name ' Pi2' is not defined 6 ... 7

#### 例題 三角形の面積 (triangle area) 3 辺の長さ a,b,c に対応する三角形の面積を計算する関数 triangle area(a,b,c) を作成せ
よ． 面積は数式 √ s(s − a)(s − b)(s − c) で与えられる．ただし s = (a + b + c)/ 2 である． 回答例 

**Python3**

```python
def triangle_area(a,b,c):
    s = (a+b+c)/2.0
    return # ここに s,a,b,c を使った面積の式を書く
```

計算の様子を知るために print や assert を用いることもできる．先に書いた文から実行される． 

**Python3**

```python
def 名前 (仮引数 1, 仮引数 2...):
```

変数 3.2.2 配列 配列は，0 個以上の式をカンマでつなげ "[" と "]" でくくって構築したものである (例: [3,4,5])． 空の配列も存在する (例: [])．

<!-- source: pdf_page=49; md_page_heading=p.50 -->

3.2 配列と関数 変数 a が配列だとして， a[i] は a の i 番目の要素を与える演算である (i は整数とする )．また len(a) は要素の数を与える． 

**実行例**

```text
>>> a = [3, 15+53, 10000]
>>> a
[3, 68, 10000]
>>> a[0]
>>> a[1]
>>> a[2]
10000
>>> a[-1] # 負の数は逆順に列を参照
10000
>>> len([10,20,30])
>>>
Help:IndexError 存在しない配列の要素にアクセスすると，IndexError が生じる >>> a[3] # a の要素数は
なので，各要素に対応する添字は 0,1,2
Traceback (most recent call last):
File "<stdin>", line 1, in <module>
IndexError: list index out of range

#### 例題 Range 配列版 ３つの整数を要素として持つ配列 a に対して，それらが a0 < a

< a
```

の条件を満たすならば True を，満たさないならば False を返す関数 in range array(a) を作成せよ．なお，今回の ように対応関係が自明の場合は，a[i] を ai と断りなく表記する． 

**Python3**

```python
def in_range_array(a):
    return # ここを埋める
```

**実行例**

```text
>>> in_range_array([3,4,5])
True
>>> in_range_array([3,-4,5])
False
```

## 3.3 変数の値の書き換えとループ不変条件 (p.50-51)

<!-- source: pdf_page=50; md_page_heading=p.51 -->

3.3 変数の値の書き換えとループ不変条件 3.3 変数の値の書き換えとループ不変条件 既に定義済の変数の値を書き換える操作を代入という． Python3 変数 = 式 1 既に紹介している 初期化との違いは，既に定義した変数の値を書き換える点である．代入前の値 にはアクセスできなくなることから，破壊的代入と呼ばれることもある．初期化と (破壊的 ) 代入 は，呼び方だけはなく実質的な区別がある．多くの言語において，定数は，初期化は可能でも代入は 許されない (文法エラーとなる )．C++では初期化にはコンストラクタ (constructor) が，代入では operator=がそれぞれ呼ばれ，ユーザ定義クラスでは動作を変えることもできる． 

**実行例**

```text
>>> s =
# 初期化
>>> s
# ( これまで勧めていた使い方)
>>> s =
# 代入: この行を境に s の値は変わる
>>> s
# さっきと違う
```

以下は，初心者をしばしば混乱させる，しかし文法に則った文である． 

**Python3**

```python
a = a +
```

代入では右辺を先に評価し，その値を左辺に代入する．つまり 

**Python3**

```python
a =
a = a +
```

であれば，a = 2+1 すなわち，a = 3 として実行される．なお，このようなケースはしばしばある ので，次のような略記法が用意されている． Python3 a += 1 # a = a+1 と同じ 1 代入は，理解に右辺値や左辺値など様々な概念を必要とする，難しい概念である． 本演習では，機会を限定して代入を用いることを勧める．その一つが，次に紹介するループと共に 用いる場合である．また，この資料では当面，代入の左辺には変数単体，または a[i] のような配列の 要素のみを扱うとする．

<!-- source: pdf_page=51; md_page_heading=p.52 -->

3.3 変数の値の書き換えとループ不変条件 

**Python3**

```python
def 名前 (仮引数 1, 仮引数 2...):
```

変数 = 初期値 < n とする．(n=10000 などで検算する)

**回答例:**  

**Python3**

```python
def sum_to_n(n):
    total =
    for i in range(1, n +1):
        total = total + i
        return total
```

このようなプログラムがどのように導かれるか追ってみよう． 仮に固定値である までの合計であれば，シンプルに記述可能である． 

**Python3**

```python
def sum3():
    return 1+2+3
```

補助変数 si (0 から i までの和) を用いて，等価なプログラムに書き直すこともできる． 

**Python3**

```python
def sum3():
    s0 =
    # 空集合の和
    s1 = s0 +
    #
```

の和 代入を用いると，補助変数 si を一つの変数 total で表現することもできる． 

**Python3**

```python
def sum3():
    total =
    # total は s0 相当
    total = total +
    # 左辺の total は s1, 右辺の total は s0 相当
    total = total +
    total = total +
```

## 3.4 配列とループ (p.52)

<!-- source: pdf_page=52; md_page_heading=p.53 -->

3.4 配列とループ return total # total は s3 相当 6 これを for で書き換えたものが，冒頭の回答例である． 

**Python3**

```python
def sum_to_n(n):
    total =
    for i in range(1, n +1):
        # (a) この時点で total の値は
```

から i-1 までの和 =i(i-1)/2 total = total + i # なお，total += i と書いても同じ 5 # (b) この時点で total の値は 0 から i までの和 =i(i+1)/2 6 return total 7 コメント (a)(b) に記述した total の値に関する条件が，ループのどの時点でも成り立つことを確認し てほしい．このような条件を，ループ不変条件という．これらの total の値に関する条件を用いて，関 数が n までの和を計算することを証明することができる． 次の 3.4 節の課題では，提出する全てのソースコードにループ不変条件を明確にしてコメントで記述 すること．なお，説明のために上記の例では (a), (b) 二つの条件を書いたが，以下の演習の回答では， 各ループ開始直後 (a) または終了直前 (b) のどちらかを記述すれば良い． Notice 

> **Notice:** Loop 不変条件の表現 動作や変更を表す言葉は避ける．静止した状態を表現すること． 例: ジャガイモの皮を剥いて... v.s. カレーライス 例: 1 を足して 2 を足して..i まで足したもの v.s. 1 から i までの和/合計値．似ているが後者は 計算手段は指定していない． 3.4 配列とループ

#### 例題 要素の和 (mysum) 配列 a に格納されているデータの合計を求める関数 mysum(a) を作成せよ． 例題 最大値 (mymax) 配列 a に格納され
ているデータの最大値を求める関数 mymax(a) を作成せよ． なお実際には，Python では，配列の最大値を返す max という関数が用意されている． (が，この例 題ではそのことは忘れるとする)

## 3.5 配列や文字列の変更 (p.53-56)

<!-- source: pdf_page=53; md_page_heading=p.54 -->

3.5 配列や文字列の変更

#### 例題 最大値の添字 (max index) 配列 a に格納されているデータの最大値の添字を求める関数 max index(a) を作成せよ． 例題 二番目に大きな値
 (second max) 配列 a に格納されているデータのなかで 2 番目に大きな値を求める関数 second max(a) を作成 せよ．

#### 問題 最小と最大を除いた合計 (score) 審判の点数が配列 a で与えられる．それをオリンピック方式 (最大と最小の点数をつけた審判 をそれぞれ一人づつ除いた残り
を使う) で合計する関数 score(a) を作成せよ．審判は 3 人以上 と仮定して良い． ループ不変条件の練習なので，max, min, sort などを使わずに書くこと．

#### 問題 各桁の和 (digit sum) 与えられた正の整数 n に対して，十進表現での各桁の和を求める関数 digit sum(n) を作成 せよ． 整数 n の桁数
は math.floor(math.log10(n))+1 で得ることができる (import math が必要)．また 10 の n 乗は 10**n で得られる． 3.5 配列や文字列の変更 合計を表す変数に数を足す変更が役に立ったように，配列に要素を追加したい場合もある． Python では append というメソッドが用意されている． 

**実行例**

```text
>>> a = [1, 2, 3]
>>> a.append(4)
>>> a
[1, 2, 3, 4] # a の指す配列の内容は変更されている
>>> a = a + [5]
>>> a
[1, 2, 3, 4, 5] # 配列同士の連結 (上記は+=と略記可)
```

Python で文字列に対しても +演算が用意されていることは既に紹介したが， +=という演算も用意さ れている．数に対する+=同様に，これらも破壊的操作である． なお，C++の配列は，C の配列に由来するもので，Python と比べて制限が大きい．代わりに標準ラ イブラリの std::vector<int>を用いると良い．

<!-- source: pdf_page=54; md_page_heading=p.55 -->

3.5 配列や文字列の変更 

**C++11**

```cpp
#include <vector>
#include <iostream>
using namespace std;
int main() {
vector<int> a = { 1,2,3 };
for (size_t i=0; i<a.size(); ++i)
cout << a[i] << endl; // 1,2,3 が一行毎に表示される
cout << endl; // 下記と区別するための空行
a.push_back(4); // a に
```

を追加 から与えられた正の整数 n までを順に要素として持つ配列を返す関数 iota を作成せよ． 

**実行例**

```text
>>> iota(5)
[1, 2, 3, 4, 5]
>>> iota(7)
[1, 2, 3, 4, 5, 6, 7]
```

ヒント: a = [] から始めて，順に a に要素を追加する． 回答例 (一部は白文字で書いてある): 

**Python3**

```python
def iota(n):
    a = []
    for i in range(1,n+1):
```

a.append(i) で 割 り 切 れ る 全 て の 数 を 含 む 配 列 を 関 数 filter multiple37(a) を作成せよ． 

**実行例**

```text
>>> filter_multiple37(iota(15))
[3, 6, 7, 9, 12, 14, 15]
```

<!-- source: pdf_page=55; md_page_heading=p.56 -->

3.5 配列や文字列の変更

**ヒント:**  if 文を用いて条件が成り立った時のみ，答えの配列に要素を加える．

#### 問題 約数列 (factor) 与えられた整数 n(> 0) の約数を全て求め，配列として返す関数 factor(n) を作成せよ．効率 は n 回の除算を行うもので
良い．

#### 問題 たきぬつきね (decrypt2) 二つの文字列 a, b に対して， 両者を先頭から2 文字づつ交互に混ぜた文字列を s とする．例え ば，a="ABCD",
 b="EFGH" の時，s="ABEFCDGH"である。この s から逆に a, b を求めたい . 与 えられた s に対応する，2 つの文字列を要素として持つ配列 [a,b] を返す関数 decrypt2(s) を 作成せよ．なお，文字列 a,b の文字数は偶数で同じとする．すなわち文字列 s の長さは 4 の倍 数と仮定して良い． 

**実行例**

```text
>>> decrypt2("takinutukine")
[' tanuki' , ' kitune' ]
>>> decrypt2(' mutirahasayamebunorutukayumimoyoma' + ' modakihikanuzumatakitunotahaganiha' ) # 百人一首
```

ヒント: 

**Python3**

```python
def decrypt2(x):
    a = ""
    b = ""
    for i in range(len(x)):
        # i が何文字目かを考えて x[i] を a または b に加える
        # a,b のループ不変条件は...
        return [a,b]
```

**C++11**

```cpp
#include <vector>
#include <string>
#include <iostream>
using namespace std;
vector<string> decrypt2(string x) {
string a="", b="";
for (size_t i=0; i<x.size(); ++i) {
```

// i が何文字目かを考えて x[i] を a または b に加える

<!-- source: pdf_page=56; md_page_heading=p.57 -->

3.5 配列や文字列の変更 auto v = decrypt2("takinutukine"); 13 cout << v[0] << endl; 14 cout << v[1] << endl; 15 } 16

#### 問題 たぬき暗号 (erase ta) 与えられた文字列 x から"ta"を取り除いた文字列に相当する文字列を作成して返す関数 erase ta(x) を作成せよ．文
字列 x は 2 文字以上とする．しかし，"ta"は，偶数文字目奇数文 字目いずれにも来るかもしれない． Python で文字列 x の i 文字目から 2 文字を取り出すには，x[i:i+2] と記述する． 実は Python には replace という置換を行うメソッドが用意されているので，実用にはそちらを使 うべきである．この演習では，テストケースの作成に活用すると良い． 

**実行例**

```text
>>> "tatotauktayotauttao".replace("ta","")
' toukyouto'
testcase 作成の指針: 比較的複雑なので，2 文字や
```

文字のケースをよくテストする． if 文の全ての 分岐を通ることが望ましい．(例: t で終わるもの終わらないもの..) 

**Python3**

```python
assert erase_ta(' fight' ) == ' fight'
assert erase_ta(' aa' ) == ' aa'
assert erase_ta(' at' ) == ' at'
assert erase_ta(' bt' ) == ' bt'
assert erase_ta(' tb' ) == ' tb'
assert erase_ta(' ta' ) == ' '
assert erase_ta(' xyz' ) == ' xyz'
assert erase_ta(' ttaa' ) == ' ta'
```

/lightbulb ヒント 「"t"ぬき暗号」であれば下記のように簡単に作成できる．"ta"を除く場合も，if 文の条件式を 変更するだけで対応できる (考えてみよう)．

## 3.6 発展: 関数のパラメータ化 (p.57-58)

<!-- source: pdf_page=57; md_page_heading=p.58 -->

3.6 発展: 関数のパラメータ化 def erase_t(x): 1 s = ' ' 2 for i in range(len(x)): 3 if ...: # x[i] が 't'でなければ s に追加 4 s += x[i] 5 # ループ不変条件: s は，x の先頭から i 文字目までの，t 以外の 文字列に相当 6 return s 7 3.6 発展: 関数のパラメータ化 関数を用いる際に，計算ごとに異なる値はパラメータとして呼び出し時に与える． 多くの言語では，関数自体をパラメータとして受け渡すことができる．大枠の計算手順の中で，小 さな計算だけが違うような場合には，その部分を表す関数をパラメータとして渡すと良い． Python の場合，関数を渡す場合に関数名を括弧を付けずに書き，受け取った関数を使う場合には通 常の関数と同様に実行する．(後者は，2.4 節でも紹介した) 

**Python3**

```python
def f_sample(x):
    return 3.0*x**2 + 2.0*x + 1.0
def value_at_zero_and_ten(f): # f に応じて何かをする関数
y0 = f(0) # f(0)
y10 = f(10) # f(10)
return [y0, y10]
```

**実行例**

```text
>>> value_at_zero_and_ten(f_sample)
[1.0, 321.0]
```

C++11 の場合，double を引数に取り double を返す関数は， function<double(double)>という 型で表す． 

**C++11**

```cpp
#include <functional>
#include <vector>
#include <iostream>
using namespace std;
double f_sample(double x) {
return 3.0*x*x + 2.0*x + 1.0;
}
```

<!-- source: pdf_page=58; md_page_heading=p.59 -->

3.6 発展: 関数のパラメータ化 vector<double> value_at_zero_and_ten(function<double(double)> f) { 8 double y0 = f(0); 9 double y10 = f(10); 10 return { y0, y10 }; 11 } 12 int main() { 13 vector<double> v = value_at_zero_and_ten(f_sample); 14 cout << v[0] << ' ' << v[1] << endl; 15 } 16

#### 問題 数値積分と関数パラメータ ⋆ (integral) 数値 x を引数に取り，対応する数値を返す関数を f とする．区間 [a, b ] ( a ≤ b) の定積
分を，近 似して求める関数を作成せよ．近似手法として，台形公式とシンプソン公式の 2 種類を使える ようにせよ． (この課題ではテストケースの代わりに) 様々な関数 f と分割数 n に応じて，答えの精度がどの ように変化するかを観察して，仮説を立てて議論せよ．実験結果と議論を，コメントにコピー ペーストするなどしてソースコードと合わせて提出せよ． 精 度 に つ い て は ，解 析 的 に 原 始 関 数(F (x)) を 求 め ら れ る 関 数(た と え ば f (x) = x2 と F (x) = 1 / 3x3) を対象に用いて，F (b) − F (a) を真の値として差を議論する． 台形公式 近似のもっとも簡便な方法は，区間を細かく分けて長方形の面積の和で近似するもので ある． 小 分 け に し た 短 冊 部 分 に つ い て ，長 方 形 を 台 形 に 変 更 す る と ，近 似 の 精 度 を 高 め る こ と が で き る ．す な わ ち ，分 割 数 をn，分 割 し た 各 区 間 の 幅 をh = ( b − a)/n , xi = a + ih と し て ， ∑ 0≤i<n h(f (xi) + f (xi+1))/ 2 を答えとする．(∑ を整理すると効率が良くなるが，定義通りに計算し ても良い) a = x0 xi xi+1 b = xn x y y = f (x) シンプソン公式 (Simpson`s rule) 直線の代わりに二次式で近似すると，さらに精度が改善する． 一般に，3 点 (a, v a), (b, v b), (c, v c) を通る 2 次の多項式は次のように一意に定まる: L(x) = (x − b)(x − c) (a − b)(a − c) va + (x − a)(x − c) (b − a)(b − c) vb + (x − a)(x − b) (c − a)(c − b) vc. a = xi, b = xi+1 として，区間内の三点 (a, f (a)), ( a+b 2 , f ( a+b 2 )), ( b, f (b)) を通る補間多項式を整理

## 3.7 この章の提出課題 (p.59)

<!-- source: pdf_page=59; md_page_heading=p.60 -->

3.7 この章の提出課題 すると，その区間の面積は以下のように近似できる: ∫ b a f (x) dx ≈ b − a 6 ( f (a) + 4f ( a + b 2 ) + f (b) ) . もし区間内で f (x) が二次以下の多項式であれば，計算誤差を無視すれば正確な値を得ることが出 来る． 3.7 この章の提出課題 以下のいずれかで合格:
-

#### 問題 (タイトル部分に ⋆ なし) を全て Python で解く (通常コース)

- ⋆ つきの問題を 1 題解く (言語指定なし) +通常の問題を 1 問以上解いてループ不変条件とユニットテストを添える

**提出条件:** 
- 各関数にテストケースが付加されていること New!
- ループ不変条件が明確にコメントで記述されていること New! (本日 3.4 節では必須，他も適宜 推奨)
- 冗長なコードがないか確認し，標準的な書き方に整えられていること (*) New!
- 指定した課題が，指定した関数名で作成されていること
- 提出ファイルは，2.A 節を参考にして，jupyter から作成した weekN-(学生証番号).ipynb とする
- 日本語 (または英語) でコメントが付記されていること : { 途中か完成品か，授業中に既に OK を 得ていればその教員名 } 学んだこと，(あれば) 他に参考にした資料や他の人の回答など、(途中で 提出する場合は何がうまくゆかずに困っているなど ) をコメントで盛り込む． (何も苦労がなかっ た場合を除き，自作したことが分かるだけの十分なエピソードを記述すること — 授業中にスタッ フに OK を得た場合は簡潔で良い) (*) の項目の一つを挙げる．コード中に 

**Python3**

```python
if a:
    return True
else:
    return False
```

という式があったら，if ブロックの中が評価されて値をとることを考えると，これは 

**Python3**

```python
return a
```

と書き換えても同じである．この書き換えは，式 a の部分が，複雑な式であっても成り立つ．した がって，この演習では後者の記法を推奨する．

## 3.A テスト駆動開発? (p.60-61)

### 3.A.1 閏年の判定 (p.60-61)

#### 失敗するテストの作成 (p.60)

<!-- source: pdf_page=60; md_page_heading=p.61 -->

3.A テスト駆動開発 ⋆ 3.A テスト駆動開発 ⋆ 機能を拡張する場合は，(1) 先にテストケースを作る (2) すぐにテストを動かしてそのテストが働い ている (i.e., 失敗する) ことを確認する (3) 関数を変更する (4) 先ほど失敗していたテストが今度は成 功する (他のテストの結果は変化がない ) ことを確認する，という手順を繰り返すと良いとされる．各 ステップを省略すると，テスト自体が間違っていたり，変更により齟齬が生まれたりといったケース で，発見が遅れうるためである． 3.A.1 閏年の判定 ここでは閏年を判定する関数を開発するという架空のストーリーを通して、関数の実装とテスト データを並行して充実させることを経験する．このようにテストを作成しながら開発すると，テスト を作成しない場合よりタイプする文字数は 2 倍以上になることが多いが，一般には十分に元が取れる． 一般にバグが混入すると解決には開発時間の 10 倍以上の時間を要することが珍しくないが，ユニット テストを併用した開発は，バグの混入する機会を減少させるためである． 失敗するテストの作成 is leap year(y) という関数の雛形を作成する．この関数の目的は，引数 y で与えられた年が閏年 かどうかを真偽値で返すことである． 

**Python3**

```python
def is_leap_year(y):
    return True # とりあえずなんでも閏年
```

この関数をテストするクラスを作製する． Python3 %%run_pytest[clean] -qq 1 def test_leap(): # 名前を"test_"で始める 2 assert is_leap_year(2015) # 2015 年は平年 3 動かすと以下のように，失敗 (F や failures に注目) が報告される． 

**実行例**

```text
F [100%]
============================ FAILURES =============================
____________________________ test_leap ____________________________
def test_leap(): # 名前を"test_"で始める
> assert is_leap_year(2015) == False # 2015 年は平年
E assert True == False
E + where True = is_leap_year(2015)
```

中頃の line 6, in test leap から，失敗したテストを含む関数と，行数を読み取る．(もっとこの

#### 実装とテストの拡充 (p.61)

#### 実装の変更 (p.61)

#### 仕様の変更 (p.61)

<!-- source: pdf_page=61; md_page_heading=p.62 -->

3.A テスト駆動開発 ⋆ 場合はテストが一つしか無いので自明である )．このように，まず間違いようがない単純な段階でテス トを意図通りに失敗させると良い．テストが実は動いていないとか，勘違いが二重に作用してテスト の不備が見過ごされるなどを防ぐため． 実装とテストの拡充 では，関数 is leap year(y) を編集して，もう少しまともに閏年が 4 年に一度であることを判定す るようにする． 

**Python3**

```python
def is_leap_year(y):
    if y %
```

== 0: 年間 をテストすること全ての剰余のケースを試す． 

**Python3**

```python
def test_leap():
    assert False == is_leap_year(2015)
    assert True == is_leap_year(2016)
    assert False == is_leap_year(2017)
    assert False == is_leap_year(2018)
```

これを実行して，テストが全て成功することを確認する． 実装の変更 ここで関数 is leap year(y) の実装が，冗長であることに気づいたとする．if 文を使う必要はない ので，以下のように書き換えてみよう． 

**Python3**

```python
def is_leap_year(y):
    return y %
```

== で割り切れる年は閏年ではない」と上司の指 示で (あるいはユーザからの指摘) で分かったとする．

## 3.B C++でのテスト (p.62-63)

<!-- source: pdf_page=62; md_page_heading=p.63 -->

3.B C++ でのテスト このような場合には，関数を変更する*前に*テストを変更する．これまでのメソッド test に追加す ることもできるが，ここでは test leap100 という関数を新たに追加し，そこに新たに assert 文を用 いてテストを記述する．このように意味のまとまり毎に関数を分けておくと，見通しが良い．ただし， メソッド名は test で始める必要がある (それらのもののみ実行される)． 

**Python3**

```python
def test_leap():
```

... # すでにあるものはそのまま 実 際 に テ ス ト が1900 年 で 失 敗 す る こ と を ，実 行 し て 確 認 す る ．こ れ に よ り 新 た に 作 成 し た test leap100 が正常に働いていることが分かる． 

**実行例**

```text
.F [100%]
============================ FAILURES =============================
__________________________ test_leap100 ___________________________
def test_leap100(): # 新たに作成
assert True == is_leap_year(1896)
> assert False == is_leap_year(1900) # メインのテスト
E assert False == True
E + where True = is_leap_year(1900)
```

この準備を経て，いよいよ関数を修正する． 

**Python3**

```python
def is_leap_year(y):
    return not (y %
```

== 0) and y % で割り切れる年であっても閏年である．適切なテスト を追加したうえで，そのような条件を含めた is leap year(y) を作成せよ． 3.B C++ でのテスト C,C++では assert という標準ライブラリを用いて，同様のテストを実現可能である．演習の範囲 では，assert で十分であろう．assert の中には，true になるはずの式を記述する．

<!-- source: pdf_page=63; md_page_heading=p.64 -->

3.B C++ でのテスト 

**C++**

```cpp
#include ... // 自分のファイルを読み込み
#include <cassert> // 追加
int main() {
assert(! is_leap_year(2014));
assert(is_leap_year(2015)); // 失敗するテスト
}
```

卒業研究やプロジェクト実習などで中規模以上のプログラムを開発する場合は，あるいは実用的に は，boost::test や gtest などの外部ライブラリを用いると，圧倒的に便利である．詳しくは各ライ ブラリのドキュメントを参照のこと．
